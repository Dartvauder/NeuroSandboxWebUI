name: 'Auto Assign'
on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]

jobs:
  add-reviews:
    runs-on: ubuntu-latest
    steps:
      - uses: kentaro-m/auto-assign-action@v2.0.0
        addAssignees: author